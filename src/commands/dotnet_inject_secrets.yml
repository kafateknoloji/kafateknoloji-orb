description: >
  This command creates a secrets.json file with the provided secret value.
parameters:
  secret_value:
    type: string
steps:
  - run:
      name: Create secrets.json
      command: <<include(scripts/dotnet/add_secrets_json.sh)>>
      environment:
        SECRET_VALUE: <<parameters.secret_value>>
