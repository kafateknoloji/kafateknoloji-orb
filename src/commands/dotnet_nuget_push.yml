description: >
  This command pushes NuGet packages located in the specified source folder to a NuGet server.
parameters:
  source_folder:
    type: string
    default: "/app/nupkgs/"
steps:
  - run:
      name: Push to NuGet
      command: <<include(scripts/dotnet/nuget_push.sh)>>
      environment:
        SOURCE_FOLDER: <<parameters.source_folder>>
