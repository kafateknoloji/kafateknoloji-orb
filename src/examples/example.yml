description: >
  Sample example description.
# Provide a use-case based example for using this orb.
# Everything in the `usage` section will be displayed in the orb registry.
# Comments are not retained.
usage:
  version: 2.1
  orbs:
    kafateknoloji: kafateknoloji/kafateknoloji-orb@1
  workflows:
    inject-version-data:
      jobs:
        - kafateknoloji/inject-version-data:
            base_version: "1.0.0"
            pipeline_id: <<pipeline.number>> # Using a built-in CircleCI variable
